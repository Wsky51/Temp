# 深度学习反向传播（Backpropagation）详解

## 0. 反向传播的核心目标
一句话概括：
**反向传播 = 链式法则 + 梯度下降**，用于计算神经网络中每个参数对最终损失函数的梯度（偏导数），进而通过梯度下降更新参数，最小化损失。

核心流程：
1. 前向传播：计算网络输出和损失值
2. 反向传播：从输出层往输入层反向计算各参数的梯度
3. 参数更新： $w = w - \eta \nabla L$, $\eta$ 为学习率， $\nabla L$ 为损失对参数的梯度）

## 1. 定义基础神经网络结构
以**两层全连接神经网络**（输入层 + 隐藏层 + 输出层）为例，是推导反向传播的最小完整单元：

| 符号 | 含义 |
|------|------|
| $$x$$ | 输入向量 |
| $$W_1, b_1$$ | 隐藏层权重矩阵、偏置向量 |
| $$W_2, b_2$$ | 输出层权重矩阵、偏置向量 |
| $$\sigma(\cdot$$) | 激活函数（如 Sigmoid/ReLU） |
| $$\hat{y}$$ | 网络预测输出 |
| $$y$$ | 真实标签 |
| $$L$$ | 损失函数（此处用 MSE） |

## 2. 前向传播公式推导
### 2.1 隐藏层计算
隐藏层的线性输出：
$$z_1 = W_1 \boldsymbol{x} + b_1$$

经过激活函数后的隐藏层输出：
$$a_1 = \sigma(z_1)$$

### 2.2 输出层计算
输出层的线性输出：
$$z_2 = W_2 a_1 + b_2$$

最终预测输出（若为分类任务可替换为 Softmax）：
$$a_2 = \hat{y} = \sigma(z_2)$$

### 2.3 损失函数（均方误差 MSE）
$$L = \frac{1}{2}(y - \hat{y})^2$$
> 注：乘以 1/2 是为了求导后抵消平方项的系数，简化计算，不影响梯度方向。

## 3. 反向传播核心：链式法则
反向传播的数学基础是**多元函数链式求导法则**，目标是求解：
$$\frac{\partial L}{\partial W_2},\ \frac{\partial L}{\partial b_2},\ \frac{\partial L}{\partial W_1},\ \frac{\partial L}{\partial b_1}$$

### 3.1 输出层梯度计算
#### 步骤1：定义输出层误差项
误差项表示损失对输出层线性输出 $z_2$ 的偏导数：
$$\delta_2 = \frac{\partial L}{\partial z_2}$$

#### 步骤2：链式展开
根据链式法则分解：
$$\frac{\partial L}{\partial z_2} = \frac{\partial L}{\partial a_2} \cdot \frac{\partial a_2}{\partial z_2}$$

#### 步骤3：逐项计算
- 损失对输出层激活值的偏导：
  $$\frac{\partial L}{\partial a_2} = \frac{\partial}{\partial a_2} \frac{1}{2}(y-a_2)^2 = -(y - a_2)$$
- 激活函数对线性输出的偏导：
  $$\frac{\partial a_2}{\partial z_2} = \sigma'(z_2)$$

#### 步骤4：输出层误差项最终形式
$$\delta_2 = -(y - a_2) \cdot \sigma'(z_2)$$

### 3.2 输出层参数 $(W_2, b_2)$的梯度
#### 权重 $W_2$ 的梯度
$$\frac{\partial L}{\partial W_2} = \frac{\partial L}{\partial z_2} \cdot \frac{\partial z_2}{\partial W_2} = \delta_2 \cdot a_1^T$$
> 注： $a_1^T\$是隐藏层输出的转置，因 $z_2 = W_2 a_1 + b_2$ 是矩阵乘法，求导需转置保证维度匹配。

#### 偏置 $b_2$ 的梯度
$$\frac{\partial L}{\partial b_2} = \frac{\partial L}{\partial z_2} \cdot \frac{\partial z_2}{\partial b_2} = \delta_2$$
> 注: $\frac{\partial z_2}{\partial b_2} = 1$，偏置的梯度等于对应层的误差项。

### 3.3 隐藏层梯度计算
#### 步骤1：定义隐藏层误差项
$$\delta_1 = \frac{\partial L}{\partial z_1}$$

#### 步骤2：链式展开
$$\frac{\partial L}{\partial z_1} = \frac{\partial L}{\partial z_2} \cdot \frac{\partial z_2}{\partial a_1} \cdot \frac{\partial a_1}{\partial z_1}$$

#### 步骤3：逐项计算
- 输出层线性输出对隐藏层激活值的偏导：
  $$\frac{\partial z_2}{\partial a_1} = W_2^T$$
- 隐藏层激活函数对线性输出的偏导：
  $$\frac{\partial a_1}{\partial z_1} = \sigma'(z_1)$$

#### 步骤4：隐藏层误差项最终形式
$$\delta_1 = (W_2^T \delta_2) \odot \sigma'(z_1)$$
> 注： $\odot$ 表示**按元素相乘（Hadamard 积）**，而非矩阵乘法，保证维度一致。

### 3.4 隐藏层参数 $(W_1, b_1)$的梯度
#### 权重 $W_1$ 的梯度
$$\frac{\partial L}{\partial W_1} = \delta_1 \cdot \boldsymbol{x}^T$$

#### 偏置 $b_1$ 的梯度
$$\frac{\partial L}{\partial b_1} = \delta_1$$

## 4. 反向传播梯度汇总
| 参数 | 梯度公式 |
|------|----------|
| 输出层误差项 | $\delta_2 = -(y - a_2) \odot \sigma'(z_2)$ |
| $W_2$ 梯度 | $\frac{\partial L}{\partial W_2} = \delta_2 a_1^T$ |
| $b_2$ 梯度 | $\frac{\partial L}{\partial b_2} = \delta_2$ |
| 隐藏层误差项 | $\delta_1 = (W_2^T \delta_2) \odot \sigma'(z_1)$ |
| $W_1$ 梯度 | $\frac{\partial L}{\partial W_1} = \delta_1 \boldsymbol{x}^T$ |
| $b_1$ 梯度 | $\frac{\partial L}{\partial b_1} = \delta_1$ |

## 5. 反向传播的核心逻辑
1. **从后往前算**：先算输出层误差，再传递到隐藏层，避免重复计算
2. **复用中间结果**：每层误差可复用前一层的计算结果，大幅提升效率
3. **维度匹配**：所有梯度计算需保证矩阵/向量维度匹配（转置、按元素乘）
4. **激活函数导数**：决定误差在层间传递的强度（如 ReLU 导数为 0/1，易导致梯度消失）

## 6. 为什么反向传播高效？
- 直接正向计算每层梯度会重复计算大量嵌套导数，时间复杂度为 $O(n^2)$
- 反向传播通过“反向传递误差”复用中间结果，时间复杂度仅为 $O(n)$ ( $n$ 为参数数量）
- 这也是深度学习能训练深层网络的核心原因之一

### 总结
1. 反向传播的本质是**链式法则的工程化应用**，核心是从输出层往输入层反向计算各参数的梯度；
2. 误差项 $\delta$是反向传播的核心载体，每层误差 = 后层误差 × 权重转置 × 激活函数导数；
3. 权重梯度 = 本层误差 × 上一层输出的转置，偏置梯度直接等于本层误差。
